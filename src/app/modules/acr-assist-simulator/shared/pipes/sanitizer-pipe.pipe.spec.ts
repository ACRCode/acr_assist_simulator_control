import { SanitizerPipePipe } from './sanitizer.pipe';

describe('SanitizerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SanitizerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
